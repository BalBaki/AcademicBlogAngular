import { PdfFilterPipe } from './pdf-filter.pipe';

describe('PdfFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PdfFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
